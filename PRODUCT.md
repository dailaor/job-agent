# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

Python 本地服务、SQLite 与无构建步骤的 HTML/CSS/JavaScript；Windows 10/11 x64 为首个便携发布目标。

## Users

面向希望集中发现、筛选和管理岗位的中文求职者。首次用户上传简历后，系统在本机提取可识别的技能、年限和学历建立初始档案；解析不完整时由用户在配置页校正。再次使用时可以沿用上次条件，也可以为本次任务替换当前简历。

## Product Purpose

Job Agent 在用户本机完成固定公共渠道的岗位采集、标准化、跨渠道去重、硬规则过滤、能力关系分类和投递台账管理。成功体验是用户能快速得到有证据的岗位列表，跳转原始官网自行完成最终投递，并清楚知道每个岗位为何通过或被过滤。

## Positioning

不是批量代投机器人，而是本地优先、决策可解释、写操作可控的求职执行工作台。产品把“人是否匹配岗位”和“岗位是否满足人的目标”分开表达，并保留每次筛选与投递状态的审计证据。

## Operating Context

- 用户从 GitHub Actions artifact（或维护者手动附加的 Release 资产）下载 Windows 便携包，解压到可写目录后在本机运行。
- 用户上传 PDF 或 DOCX 简历；托管副本、配置、岗位和投递记录只保存在本机。
- 日常任务以“可选替换简历 → 沿用或调整硬规则 → 选择本次渠道 → 获取并评估岗位”为主。
- 渠道适配器只把具有真实岗位字段和可用详情链接的记录送入评估。

## Capabilities and Constraints

- 支持规则模式；BYOK 与本地 OpenAI-compatible 模型接口属于后续匹配能力的扩展点。
- 基础便携版不处理验证码、网站自动登录、Cookie/Token、邮箱登录和登录后官网状态同步，也不捆绑 Playwright/Chromium。
- 遇到登录、验证码、空白 SPA、缺少稳定详情或页面结构变化时停止当前渠道并给出明确原因。
- 默认用户流程跳转原始官网，由用户完成最终提交；源码中的实验写执行器默认关闭、不确定时不得重试。
- 渠道必须通过注册表扩展：发现、评估和清单生成无需修改主流程；只有专用多阶段写协议需要独立执行器。

## Evidence on Hand

- 工作区已有可运行的 Python 原型、SQLite 数据模型、本地 Web 控制台和覆盖核心链路的离线测试。
- 已验证固定渠道采集、跨渠道去重、硬规则、冲高/持平/保底分桶、投递状态机与审计台账的离线链路。
- 真实公共渠道的当前可用性仍需逐个适配器验证，不得从演示数据或入口页面推断。

## Product Principles

- 首次可用优先：模型、插件和高级筛选不阻塞第一次得到结果。
- 证据优先：入口可访问不等于已提取真实岗位。
- 渐进复杂度：岗位和地点在主流程中突出，其他硬规则在高级区随时可调。
- 可恢复：单渠道或模型失败不应抹掉其他结果。
- 本地可维护：配置、规则、渠道与数据结构都有稳定文档和测试入口；模型提示词在 BYOK/Ollama 功能落地时再作为独立、可版本化模块加入。

## Accessibility & Inclusion

关键流程必须支持键盘操作、明确焦点、可读错误信息和移动宽度重排；状态不能只依赖颜色表达。
