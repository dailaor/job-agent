# 求职 Agent 验收标准

## 产品边界

本项目是一个单用户、本地优先、固定渠道的求职 Agent。它使用用户上传的当前简历和明确配置的条件，不按岗位改写简历，不生成求职信或开放题答案，也不尝试绕过登录、验证码或平台限制。默认流程跳转原始岗位页，由用户完成最终投递。

## 必须完成的能力

1. 首次可上传 PDF/DOCX；再次使用可沿用当前简历或选择新文件覆盖。可读文本在本机提取明确技能、年限和学历，无法识别时允许手动校正。
2. 岗位先做字段证据校验、标准化、跨渠道去重、过期检查和扩展硬规则过滤，再分别计算“人匹配岗位”和“岗位满足人”。
3. 能力关系只分为“冲高、持平、保底、不投”；冲刺/均衡/保底同时调整双向权重和每日清单配额，不能绕过硬规则。
4. 渠道通过注册表扩展；普通新渠道无需修改数据库、编排器和页面即可发现、评估并进入清单。越过 HTTPS 域名白名单的记录必须拒绝。
5. 统一岗位池提供原始详情/申请页跳转，SQLite 记录岗位、评估、清单、状态、事件和渠道健康度；同一岗位只进入一次清单。
6. 登录、验证码、未知必填项、开放题、页面变化和不确定副作用停止自动流程；不确定写操作禁止自动重试。
7. 提供本地 Web 控制台、命令行、演示数据、维护文档和不访问真实网站的离线测试。
8. Windows x64 基础便携包解压可运行；不捆绑 Playwright/Chromium，并明确浏览器型渠道边界。

## 明确不承诺

- 未配置站点定义的任意公司官网不会被自动适配。
- 未在真实账号和最新页面中冒烟测试前，不声称 BOSS 或某个官网当前选择器可用。
- 基础版本不承诺 BYOK/Ollama、大模型提示词、浏览器扩展、自动登录、邮箱登录或登录后官网状态同步。
- 系统不接收账号密码，不破解验证码，不规避站点限速或风控。
